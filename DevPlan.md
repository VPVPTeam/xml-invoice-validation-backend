# Dev Plan — XML Invoice Validation Backend

Форвард-лукинг роадмап. **Сверяться перед добавлением любой новой фичи** —
чтобы архитектура закладывалась под будущие направления и не переделывалась.

## Подход к разработке (резюме-ориентированный)

Приоритет — фичи, которые **отдельной строкой идут в резюме и обсуждаются на
собеседовании**: тесты, Docker, CI/CD, облако, многопоточность, очереди сообщений.
Это переносимые технические навыки и инструменты, востребованные повсеместно.

Доменные фичи (RuleGroup, common/private правила и т.п.) marketable-навыком не
являются — их нельзя показать в CV или на собесе, поэтому они **отложены** в раздел
«ХЗ когда» и делаются только после основной, резюме-значимой части.

**Рабочий цикл для каждой фичи:**
1. Реализовать фичу.
2. Написать под неё тесты.
3. Смерджить в `dev`.
4. Убедиться, что pipeline зелёный и версия задеплоена.

Docker, CI/CD и облако готовы, поэтому пункт 4 теперь выполним целиком.

## Статус

- **Аутентификация — сделана.** Пользователи, роли, JWT, вход, `/me`, создание
  пользователей администратором, ограничения доступа.
- **Глобальные (интеграционные) тесты — сделаны.** 53 теста, Testcontainers +
  PostgreSQL в Docker. Покрыты все эндпоинты всех контроллеров.
- **Рефакторинг — сделан** (ветка `refactor/cleanup`). Список найденных проблем
  закрыт полностью, интеграционные тесты не пришлось править ни разу. Итоги — ниже.
- **Точечные юнит-тесты — сделаны** (ветка `test/unit-tests`): семь классов,
  Spring и база не поднимаются. Состав — ниже.
- **Docker — сделан** (ветка `feature/docker`): multi-stage образ, `docker-compose`
  с PostgreSQL. Итог — ниже.
- **CI/CD — сделан** (ветка `feature/ci`): GitHub Actions, сборка + весь набор
  тестов на push в `dev`/`main` и на каждый PR. В этой же ветке тестовые фактуры
  переведены на синтетические (см. «Тестовая инфраструктура»).
- **Cloud — сделан** (ветка `feature/cloud`): приложение задеплоено на AWS —
  EC2 (Docker-контейнер) + образ в ECR + managed PostgreSQL (RDS), регион
  eu-central-1. Деплой пока ручной; доработки вынесены в «ХЗ когда». Справка по
  инфраструктуре — ниже и в `docs/DEPLOY.md`.
- **Текущий этап — многопоточность** (пункт 5 очереди MVP+), затем Kafka. Это
  последние две резюме-значимые фичи.

---

# Рефакторинг — принципы и итоги

## Принципы (пригодятся при следующем заходе)

- **Тесты — страховочная сеть.** Они не зависят от внутренней структуры классов,
  поэтому рефакторинг их ломать не должен. Если сломались — сначала проверить,
  не утверждает ли тест деталь реализации вместо поведения.
- **Прогон всех тестов после каждого шага**, а не в конце: тогда причина падения
  известна сразу.
- **Отдельный коммит на каждый шаг** — неудачный шаг откатывается один.
- **Размер метода — симптом, а не диагноз.** Резать по числу строк нельзя:
  получится дробление, при котором один сценарий приходится собирать из десятка
  трёхстрочных методов. Настоящий критерий — **смешение уровней абстракции** и
  **несколько причин для изменения**.
- **Абстракции выделять по необходимости, а не ради выделения.** Интерфейс или
  абстрактный класс оправданы, когда есть вторая реализация либо реальная
  потребность развязать зависимость. Интерфейс с единственной реализацией —
  спекулятивное обобщение. Оправданные абстракции в проекте уже есть:
  `FormatProcessor` и `TechnicalValidator<T>` (за ними стоит требование
  поддержать несколько форматов).
- **Резать класс по независимым причинам для изменения, а не по объёму.** Ось
  разделения выбирается осознанно: у сервисов персистентности их две —
  «чтение/запись» и «по сущностям», обе применены. У маппера независимых причин
  нет (нет состояния, нет зависимостей, нет ветвлений) — он оставлен одним
  классом сознательно, разделим при переходе на MapStruct или с приходом RuleGroup.
- **Порядок работ:** сначала приводить в порядок содержимое строк, потом
  переставлять строки по методам. Перемещать чистый код дешевле, чем повторно
  резать грязный.

## Как теперь устроен код (итоги)

- **Идентичность фактуры — тип `InvoiceId`** (`sellerTaxId` + `invoiceNumber`,
  метод `value()`). Единственный источник строкового представления; `InvoiceId.NONE`
  для случаев, когда фактура не опознана.
- **`ValidationIssue` создаётся только фабриками** `technicalError` и
  `businessWarning` — они держат связку этап/severity. Конструктор приватный.
- **Неизменяемые результаты с фабриками под два пути создания.** `ValidationOutput`
  (`of` / `restored`), `ValidationBatch` (`created` / `restored`),
  `TechnicalValidationOutput` (`success` / `failure`). Сеттеров нет; «посчитали
  при валидации» и «прочитали из базы» разведены по именам.
- **`BatchTotals`** — record со счётчиками и статическим `compute`, вся арифметика
  вынесена из `ValidationOutput`.
- **`BatchAccumulator`** (package-private, пакет `service`) — собирающий параметр:
  копит нарушения, идентификаторы и счётчик дубликатов, в `toOutput()` сам создаёт
  батч и результат.
- **`TechnicalIssueCollector`** (package-private, пакет `validator.technical`) —
  держит имя файла и `InvoiceId`, поэтому каждая проверка указывает только своё
  условие и путь поля.
- **`RuleKeys`** — все системные ключи правил в одном месте. Бизнес-ключи задаёт
  пользователь, в коде их нет.
- **`XmlFileDataReader`** (package-private, пакет `controller`) — превращает
  `MultipartFile` в `XmlFileData`. `MultipartFile` в сервисный слой не проникает.
- **Сервисы записи разделены по сущностям:** `ValidationPersistenceService`
  (только результат валидации), `VendorPersistenceService`,
  `BusinessRulePersistenceService`. Чтение по-прежнему всё в `ValidationQueryService`.
- **`PersistenceMapper`** — один маппер на слой персистентности (переименован из
  `ValidationPersistenceMapper`, разделять не стали).
- **Контроллеры не ловят `Exception`.** Единственный `try/catch` в веб-слое —
  вокруг чтения байтов загруженного файла (`IOException`). Всё неожиданное
  доходит до `handleUnexpected`: 500 плюс запись в лог.

---

## MVP+ — очередь работ

### 1. Точечные юнит-тесты — сделано

Покрыто стабильное ядро — то, что не перепишут ни ближайшие фичи (многопоточность,
очереди), ни отложенные (RuleGroup, per-user правила). Все семь классов работают
без Spring и без базы.

- **`OperatorEvaluatorTest`** — все восемь операторов, регистронезависимость,
  границы `BETWEEN`, разделители `;` и `,`, нечисловые значения, числовое
  сравнение вместо текстового. Отдельно — три параметризованных теста по всему
  перечислению `RuleOperator`: класс никогда не бросает и не пропускает `null`.
  По ходу закрыта дыра: `expectedValue = null` роняла `GREATER_THAN`, `BETWEEN`,
  `IN`, `NOT_IN`, `CONTAINS` и сам `isExpectedValueValid`.
- **`BatchTotalsTest`** — комбинаторика счётчиков: несколько нарушений на одной
  фактуре считаются один раз, дубликаты не попадают в `invoicesWithIssues`,
  нарушение неопознанной фактуры не портит счётчики остальных.
- **`KsefTechnicalValidatorTest`** — около тридцати запусков: параметризованный
  список всех обязательных полей (`@MethodSource` с лямбдами-поломками),
  отсечение вложенных проверок при отсутствии родительского объекта, индексы
  строк фактуры, откат идентичности к `UNKNOWN`.
- **`FieldValueExtractorTest`** — значение по пути, пустое поле, отсутствующий
  промежуточный объект, неподдерживаемый и `null` путь.
- **`XmlFileDataReaderTest`** — три ветки отказа, подстановка `UNKNOWN`,
  сохранение порядка и содержимого файлов.
- **`PersistenceMapperTest`** — полнота маппинга, обратная ссылка `addIssue`,
  восстановление счётчиков «как сохранено», неперенос `id` вендора. Маппинг
  правил не покрыт намеренно — его перепишет RuleGroup.
- **`UserServiceTest`** — единственный с Mockito: в базу уходит хеш, а не пароль;
  занятый email не доходит ни до кодировщика, ни до `save`.

**Не покрываем сознательно:** `ValidationService` (будет переписан под
многопоточность, поведение держат интеграционные тесты), `BusinessValidator`
(его цикл переписывает RuleGroup), маппинг правил, классы сообщений, `RuleKeys`,
`CanonicalFieldRegistry`, записи и DTO.

**Осталось непокрытым осознанно:** ветка `TECH_CANONICAL_MAPPING_FAILED` в
`KsefTechnicalValidator` — настоящий маппер её не воспроизводит (он падает только
на отсутствующих полях, а до него доходят лишь полные фактуры), а `KsefInvoiceMapper`
объявлен `final`, поэтому нужен мок. Сделать, когда понадобится трогать эту ветку.

### 2. Docker — сделано

- **Multi-stage `Dockerfile`:** первый этап (`maven:3.9-eclipse-temurin-21`)
  собирает `jar` с `-DskipTests`, второй (`eclipse-temurin:21-jre`) несёт только
  JRE и запускается от непривилегированного пользователя. Инструменты сборки в
  финальный образ не попадают.
- **`docker-compose.yml`:** приложение + PostgreSQL 17 в одной сети. Приложение
  ходит к базе по имени сервиса `db`, не по `localhost`.
- **Схему накатывает база, а не приложение.** `schema.sql` смонтирован в
  `/docker-entrypoint-initdb.d/` — PostgreSQL прогоняет его один раз при первом
  старте пустого тома, ДО того как приложение подключится (нужно из-за
  `ddl-auto: validate`).
- **Тесты внутри сборки образа не гоняются** — им нужен Docker-демон
  (Testcontainers), а внутри сборки его нет. Проверка качества — задача CI.

### 3. CI/CD — сделано

- **GitHub Actions**, файл `.github/workflows/ci.yml`. Две задачи:
  `build-and-test` (setup JDK 21 + кеш Maven + `mvn -B verify`) и `build-image`
  (сборка Docker-образа, гейт `needs: build-and-test`).
- **Триггеры:** push в `dev`/`main` и любой pull request. Прямой push в
  feature-ветку без PR pipeline НЕ запускает — это осознанно, чтобы не гонять
  дважды; проверка feature-ветки идёт через её PR.
- **Testcontainers заводится без донастройки:** на раннере `ubuntu-latest`
  Docker предустановлен, поэтому интеграционные тесты поднимают PostgreSQL сами.

### 4. Cloud — сделано

Приложение живёт на AWS: EC2 (`invoice-server`, Amazon Linux 2023, Docker), образ
на ECR, база — RDS PostgreSQL 18. Деплой пока ручной (build+push образа с Mac →
`pull` и перезапуск контейнера на EC2). App Runner оказался закрыт для новых
клиентов (сворачивается с апреля 2026) — ушли на EC2. Детали инфраструктуры — в
разделе «Дизайн: Cloud/AWS» и в `docs/DEPLOY.md`.

### 5. Многопоточность

Параллельная обработка файлов внутри батча. Разбор одного файла уже вынесен в
отдельный метод (`ValidationService.validateFile`), а накопление результата — в
`BatchAccumulator`, поэтому распараллеливание сводится к замене цикла на
параллельный и синхронизации аккумулятора (либо сбор результатов по файлам и
слияние в конце). Под фичу — тесты.

### 6. Очереди сообщений (Kafka / RabbitMQ)

Асинхронная обработка: приём батча кладёт задачу в очередь, обработчик забирает и
валидирует в фоне. `ValidationService` уже бросает `EmptyBatchException` /
`UnsupportedFormatException` как ошибки вызывающего — рассчитано на вызов не только
из контроллера, но и из consumer'а.

**Это две последние резюме-значимые фичи.** Всё остальное — в «ХЗ когда».

## ХЗ когда (после резюме-части)

### В первую очередь
- **RuleGroup** (дизайн ниже):
  - при добавлении правила гасить предыдущее активное при конфликте по
    `(vendor_id, field_path)`, сообщать пользователю (`BusinessRuleSaveResult`);
  - несколько правил на одно поле;
  - в отчёт добавить, по каким правилам шла бизнес-валидация;
  - вместе с ним — юнит-тесты на новый `BusinessValidator`.
- Таблица правил в разрезе юзеров: common / private.

### Cloud — доработки (после многопоточности и очередей)
- **Автодеплой при пуше** (GitHub Actions → сборка образа → push в ECR →
  удалённый redeploy на EC2 через SSH или AWS SSM). Дотягивает рабочий цикл до
  конца: запушил → само собралось, протестилось, выкатилось.
- **Elastic IP** — постоянный публичный адрес (сейчас IP меняется при Stop/Start).
- **Swap-файл на EC2** — 1 ГБ RAM впритык для Spring Boot.
- **HTTPS + домен** (Nginx/Caddy + Let's Encrypt) вместо голого `http://IP`.
- **Секреты в AWS SSM / Secrets Manager** вместо `app.env`; сменить пароли базы и
  `JWT_SECRET` (сейчас дефолтный `dev-…`).
- **Health-эндпоинт (Spring Actuator) + мониторинг.**

### Доп. фичи и «когда весь код готов»
- **Документация кода** — делать в самом конце, ближе к подаче на практики
  (ориентир ~полгода), когда код стабилизируется.
- **Кастомные аннотации** — не резюме-значимо, отложено.
- Полное покрытие юнит-тестами остальных классов.
- Больше GET-эндпоинтов.
- Логи (structured logging).
- Мапперы → MapStruct (?) — не тривиально: Lombok-интеграция
  (annotationProcessorPaths + lombok-mapstruct-binding) и нестандартные маппинги
  (два аргумента, двунаправленный `addIssue`, фабрики вместо сеттеров).
  При переходе `PersistenceMapper` придётся разделить по сущностям.
- Refresh-токен для аутентификации.
- Мягкое удаление (soft delete) для Vendor (сейчас `ON DELETE CASCADE` стирает
  правила безвозвратно). Точка приложения — `VendorPersistenceService`.
- Сохранять `message` у `ValidationIssue` в базу (сейчас при чтении отчёта пустое):
  колонка в `validation_issue`, поле в entity, строка в `PersistenceMapper`.
  Тест `PersistenceMapperTest.toModel_issue_losesMessageBecauseItIsNotPersisted`
  фиксирует нынешнее поведение и упадёт, когда поведение изменится.
- Счётчик нечитаемых файлов: файл с ошибкой разбора не попадает ни в один счётчик
  (`totalInvoices`, `invoicesWithIssues`), потому что фактуру в нём опознать не
  удалось — по цифрам батч выглядит благополучным, правду говорят только
  `status: ERROR` и список `issues`. Лечится пятым счётчиком `unreadableFiles`:
  «5 фактур обработано, 1 файл не прочитан». Поведение зафиксировано тестами
  `BatchTotalsTest.compute_withIssueOnUnknownInvoice_ignoresIt` (сломанный файл
  рядом с нормальными) и `ValidationIntegrationTest.validate_withMalformedXml_reportsParseErrorIssue`
  (батч из одного сломанного файла).
- `FieldValueExtractor` ловит `NullPointerException` от лямбд реестра и возвращает
  `null` — это осознанная «безопасная навигация», а не недосмотр: явные проверки
  сделали бы каждую из четырнадцати лямбд `CanonicalFieldRegistry` вчетверо
  длиннее. Цена — `extract(null, path)` молча вернёт `null` вместо падения, хотя
  это уже ошибка программиста. Если реестр вырастет или лямбды усложнятся —
  заменить перехват на явную навигацию.
- `JwtAuthenticationFilter.authenticate` глотает `UsernameNotFoundException`
  пустым `catch` без лога — против правила «немых ошибок быть не должно».
  Логировать или убрать ветку (это «токен валиден, но пользователя удалили»).
- `show_sql: true` шумит в логах контейнера — вынести в dev-профиль/логгер и
  выключить по умолчанию.
- `open-in-view` включён по умолчанию и load-bearing: контроллеры маппят ленивые
  сущности вне транзакции (`getReport`). Чтобы выключить — перенести маппинг
  чтения в `ValidationQueryService` под `@Transactional`. Не трогать, пока не
  готовы к этому.
- Диалект Hibernate прописан в `application.yaml` явно, хотя определяется сам —
  строку `hibernate.dialect` можно удалить (предупреждение `HHH90000025`).

## Дизайн: RuleGroup (целевая модель)

- Rule = группа условий: `(vendor, name/key, is_active, combinator AND|OR, conditions[])`.
- Condition = `(field_path, operator, expected_value)` — нынешнее атомарное правило.
- Верхний уровень: все активные группы соединяются через AND. Внутри группы —
  её комбинатор. **Один уровень вложенности, без полного булева дерева.**
- Богатые операторы (`IN`, `BETWEEN`) уже покрывают «ИЛИ внутри поля» и диапазоны —
  не плодить условия зря.
- Активация и версионирование — на уровне группы (не per-field).
- **DRY: атомарный движок (`OperatorEvaluator`, `FieldValueExtractor`,
  `CanonicalFieldRegistry`) переиспользуется без изменений**; новое — только тонкий
  слой композиции результатов условий. Именно поэтому эти классы покрыты
  юнит-тестами уже сейчас — тесты переживут переход.
- Репортинг: одно нарушение на упавшую группу (гасим шум предупреждений).
- У каждой RuleGroup будет свой овнер; ключ активной уникальности обрастёт `owner`
  → `(owner, vendor_id, field_path)`.
- Переписываются два файла: `BusinessRulePersistenceService` и `BusinessValidator`.

## Дизайн: Auth (реализовано, справочно)

- Таблица `app_user`, роли одной колонкой, пароли — BCrypt.
- JWT: один access-токен (~1 час), stateless; фильтр подгружает пользователя из
  базы, поэтому деактивированный не проходит даже с живым токеном.
- Первый администратор — через сеятель (`AdminSeeder`) из конфигурации.
- Vendor: создание, изменение, удаление — только ADMIN.
- Отложено: refresh-токен; владение правилами (часть RuleGroup).

## Дизайн: Cloud/AWS (реализовано, справочно)

Полная шпаргалка по серверу — в `docs/DEPLOY.md`. Для будущих доработок готов
отдельный подробный промпт (передавать новому чату по Cloud). Регион `eu-central-1`.

- **Аккаунт:** новый AWS **Free plan** (кредитная модель): кредиты на срок до 6
  месяцев, по исчерпании аккаунт сам закрывается, с карты не списывает (пока не
  Paid). **App Runner недоступен** (закрыт для новых клиентов) — не предлагать.
  Часть сервисов на Free plan ограничена.
- **RDS PostgreSQL 18** — `invoice-db`, `db.t4g.micro`, база
  `xml_invoice_validation_backend_db`, user `postgres`, publicly accessible. SG
  `invoice-db-sg` пускает 5432 с двух IP разработчиков + приватный IP EC2
  (`172.31.33.55/32`). Схема залита вручную через `psql` (из-за `ddl-auto: validate`).
- **ECR** — приватный репозиторий `xml-invoice-validation-backend` с образом
  приложения.
- **EC2** — `invoice-server`, `t3.micro`, Amazon Linux 2023, **x86_64**. Публичный
  IP меняется при Stop/Start (Elastic IP пока нет). SG `invoice-server-sg`: SSH (22)
  с двух IP, порт 8080 открыт миру. IAM-роль `ec2-ecr-read` для pull из ECR. Docker
  с автозапуском; контейнер `invoice-app` из образа, переменные из `app.env`,
  `--restart unless-stopped`.
- **IAM-пользователь `cli-user`** (`AdministratorAccess`) + access-ключи — для
  локального `aws cli` (login/push в ECR).
- **Деплой (ручной):** на Mac `docker build --platform linux/amd64` → push в ECR →
  SSH на сервер → `pull` → пересоздать контейнер.

## Тестовая инфраструктура (как устроена)

- **Где лежат тесты.** Юнит-тест лежит в пакете тестируемого класса — это не
  вкусовщина: `XmlFileDataReader`, `BatchAccumulator` и `TechnicalIssueCollector`
  объявлены package-private, из другого пакета их не видно. Интеграционные тесты
  лежат уровнем выше, в пакете фичи (`validation`, `auth`), потому что проверяют
  не класс, а сквозной путь запроса через HTTP, безопасность, сервисы и базу.
  `AbstractIntegrationTest` — в корне тестового дерева, он общий для обеих фич.
- **Один контейнер PostgreSQL на весь прогон** — запускается в статическом блоке
  `AbstractIntegrationTest`; аннотации `@Testcontainers` / `@Container` НЕ
  используются. Причина: они гасят контейнер после каждого класса, а контекст
  Spring кешируется — второй класс получал бы мёртвое соединение.
- **Схема** подаётся через `withInitScript("schema.sql")` из `src/test/resources`.
  Корневой `schema.sql` (он же init-скрипт Docker) начинается с `DROP TABLE IF
  EXISTS` — блока `TRUNCATE` в начале больше нет (он падал на пустой базе; см.
  грабли).
- **Синтетические тестовые фактуры.** XML лежат в `src/test/resources/invoices/`
  и закоммичены: `valid-invoice.xml`, `duplicate-invoice.xml`, `invalid-invoice.xml`,
  `vendor-b-invoice.xml`, `vendor-c-invoice.xml`, `vendor-d-invoice.xml`. Данные
  выдуманы (компании — `Vendor A/B/…`, товары — `Test product`). Реальные фактуры
  в репозиторий не кладём: тесты должны быть self-contained и не зависеть от
  конфиденциальных данных, иначе CI и клон репозитория ломаются.
- **Очистка между тестами** (`@BeforeEach`): `TRUNCATE` таблиц валидации, правил и
  вендоров + удаление всех пользователей, кроме администратора (его создаёт сеятель
  один раз при старте контекста, восстановить иначе нечем).
- **`AbstractIntegrationTest`** даёт наследникам: `mockMvc`, `adminToken()`,
  `userToken()`, `tokenFor()`, `bearer(token)`, `authorizedGet()`, очистку базы.
- **`InvoiceFixture`** — запись с данными тестовых фактур (файл, налоговый номер,
  номер фактуры, название вендора) и методом `invoiceId()`.
- **Юнит-тесты Spring не поднимают вообще:** объект создаётся через `new`,
  зависимости либо настоящие (если дёшевы и без состояния — `KsefInvoiceMapper`,
  `UserMapper`), либо подменяются моками (репозиторий, `PasswordEncoder`).

### Правила написания тестов

- **Тесты не привязаны к внутренней структуре** — только к API и ответам.
  Проверено на практике: за семь шагов рефакторинга ни один тест не пришлось
  править.
- **Достаточность:** основной сценарий + все ошибки, которые код обрабатывает явно.
  Не гнаться за недостижимыми защитными ветками.
- **`401`:** один тест на контроллер, не на каждый эндпоинт. Общее правило
  `anyRequest().authenticated()` одно на приложение, а исключения (`permitAll`)
  добавляют по префиксу пути, то есть фактически по контроллеру.
- **`403`:** тест на каждый метод с `@PreAuthorize` — аннотация стоит на методах
  по отдельности, и пропажу одной поймает только её собственный тест.
- **Утверждения:** проверять итог сценария (статус, нарушения, счётчики), а не всё
  подряд — избыточные проверки делают тесты хрупкими. Тексты сообщений об ошибках
  фиксировать точно; формулировки, которые могут меняться, — через вхождение
  подстроки.
- **Record сравнивать целиком** (`isEqualTo(new BatchTotals(...))`), а не по полям:
  компилятор даёт `equals` бесплатно, при падении видно всю запись, и нельзя
  забыть проверить одно из полей.
- **Подготовка данных — через репозитории**, а не через API: иначе поломка CRUD
  роняет чужие тесты и путает диагностику.
- **Порядок элементов не утверждать** — использовать `containsInAnyOrder`.
- **DAMP важнее DRY:** выносить в помощники подготовку (строители DTO, настройку
  моков), но не ожидаемые значения — тест должен читаться сверху вниз без
  переходов по файлу.
- **Тест, фиксирующий известную дыру, называть так, чтобы это было видно**
  (`toModel_issue_losesMessageBecauseItIsNotPersisted`). Когда дыру закроют, тест
  упадёт и напомнит о себе.

## Технические нюансы — окружение и фреймворк (грабли)

- **Spring Boot 4 / Spring MVC 7:** маршруты матчатся БЕЗ трейлинг-слеша.
  `/api/vendors/` ≠ `/api/vendors` → 500 (`NoResourceFoundException`).
- **Jackson XML — это `tools.jackson.*` (Jackson 3.x), НЕ `com.fasterxml.jackson.*`.**
- **`@AutoConfigureMockMvc` в Boot 4 переехал** в модуль `spring-boot-webmvc-test`,
  пакет `org.springframework.boot.webmvc.test.autoconfigure`.
- **Testcontainers не управляется BOM Spring Boot** — версию задавать самим
  (свойство `testcontainers.version`).
- **`ddl-auto: validate`:** Hibernate таблицы НЕ создаёт и НЕ проверяет строго
  nullability. Схему обязан накатить кто-то до старта приложения: в Docker —
  `schema.sql` через `/docker-entrypoint-initdb.d/`; в облаке (RDS) — вручную
  через `psql`.
- **Init-скрипты PostgreSQL выполняются один раз** — при создании пустого тома.
  Поменяли `schema.sql` — чтобы применился, стереть том (`docker compose down -v`).
- **`TRUNCATE` на несуществующей таблице падает** (в отличие от `DROP TABLE IF
  EXISTS`) — на свежей базе init-скрипт с `TRUNCATE` в начале рушит инициализацию.
- **Переменные окружения перекрывают `application.yaml`** (relaxed binding:
  `SPRING_DATASOURCE_URL` → `spring.datasource.url`). Так конфигурируем и Docker,
  и облако (env в compose / `app.env` на EC2) — сам `application.yaml` не правим.
- **GitHub Actions:** триггеры фильтруются — при `on.push.branches: [dev, main]`
  push в feature-ветку не запускает workflow; проверка идёт через PR. Раннер
  `ubuntu-latest` несёт Docker-демон, поэтому Testcontainers работает из коробки.
- **AWS:** образ для EC2 собирать под **`linux/amd64`** (Mac'и на ARM — без флага
  образ не запустится). App Runner закрыт для новых клиентов. Free plan ограничивает
  часть сервисов и сам закрывает аккаунт по исчерпании кредитов. Публичный IP EC2
  меняется при Stop/Start (нужен Elastic IP).
- **PostgreSQL `TIMESTAMPTZ` в нативном запросе приходит как `java.time.Instant`,
  не `OffsetDateTime`.** Конвертить: `((Instant) row).atOffset(ZoneOffset.UTC)`.
- **Порядок flush у Hibernate:** на коммите сначала INSERT, потом UPDATE, потом
  DELETE. Если констрейнт требует «сначала обновить, потом вставить» — форсить
  `repository.flush()`.
- **Spring Data никогда не возвращает `null`** из методов, построенных по имени:
  коллекция → пустая коллекция, `Optional` → `Optional.empty()`. `null` возможен
  только если объявить возврат самой сущности (`Entity findBy...`) — такой формы
  в проекте нет.
- **`@ExceptionHandler` принимает массив классов** — один метод обслуживает
  несколько типов исключений; тип параметра должен быть их общим предком.
- **`new BigDecimal(null)` бросает `NullPointerException`, а не
  `NumberFormatException`** — `catch (NumberFormatException)` его не поймает.
  Проверять на `null` до вызова, а не ловить `NullPointerException`.
- **Lombok + boolean:** поле называть `active` (не `isActive`).
- **AOP-прокси:** бины с `@Transactional` / `@PreAuthorize` / `@Async` НЕ должны
  быть `final`. Классы без них (мапперы, `ValidationService`, контроллеры без
  `@PreAuthorize`) — можно и нужно.
- **Lombok `@AllArgsConstructor` несовместим с `@PersistenceContext`-полем:**
  `EntityManager` попал бы в конструктор. В таких классах конструктор писать руками.
- **Spring Security 6+:** отказ от `@PreAuthorize` бросает `AccessDeniedException`
  во время вызова метода (не в фильтре) → ловится в `ApiExceptionHandler` (403).
  Неаутентифицированный запрос без явного `AuthenticationEntryPoint` даёт 403,
  а не 401 — нужен entry point с 401.
- **Перегрузка + переменное число аргументов** легко даёт неоднозначность: вызов
  с одним аргументом подходит и `f(String...)`, и `f(String, T...)` (второй —
  с пустым списком). Проще давать разные имена.
- **JWT (jjwt 0.13):** сериализатор `jjwt-gson` (независим от Jackson); секрет
  ≥ 32 символов для HS256, брать из переменной окружения.
- **`List.of` не принимает `null`-элементы** — в тестах, где `null` нужен внутри
  списка, использовать `Arrays.asList` или `new ArrayList<>`.
- **`MockitoExtension` работает в строгом режиме:** заглушка, которую тест ни разу
  не использовал, роняет тест. `@InjectMocks` подставляет только моки — если часть
  зависимостей настоящая, собирать объект руками в `@BeforeEach`.
- **Локальная копия репозитория может отставать от ветки.** Перед правками
  сверяться с содержимым удалённой ветки, а не с локальными файлами.

## Доменные решения и конвенции

- **`invoiceId` = `sellerTaxId + "|" + invoiceNumber`** — собирается только через
  `InvoiceId.value()`; при отсутствии налогового номера продавца подставляется
  `UNKNOWN`, при неопознанной фактуре — `InvoiceId.NONE`.
- **`CanonicalFieldRegistry` — единственный источник правды по `field_path`.**
- **Системные ключи правил — только в `RuleKeys`.** Бизнес-ключи задаёт
  пользователь (колонка `business_rule.rule_key`), в коде они не перечисляются.
- **Операторы:** разделитель `BETWEEN` = `;`, `IN`/`NOT_IN` = `,`; строковые
  операторы регистронезависимы; `OperatorEvaluator` НИКОГДА не бросает —
  безопасный парсинг через `Optional`, `null` в любом из аргументов даёт `false`.
- **Двухэтапная валидация:** бизнес-этап запускается ТОЛЬКО после успешной
  технической проверки.
- **Ошибка разбора XML не роняет батч** — превращается в нарушение
  `TECH_XML_PARSE_ERROR`, остальные файлы обрабатываются.
- **Пустой пакет и неизвестный формат — ошибки вызывающего, не отчёт.**
  `EmptyBatchException` и `UnsupportedFormatException`, оба → 400. Проверки живут
  в `ValidationService`, а не в контроллере, потому что сервис будут звать и из
  очереди сообщений.
- **Отсутствие родительского объекта отсекает вложенные проверки:** нет
  `Podmiot1` — одно нарушение, а не пять. Иначе отчёт тонет в шуме.
- **Severity бизнес-issue = WARNING**, никогда ERROR.
- **Дубликаты:** in-batch и cross-batch (DAO), оба WARNING; cross-batch не
  прерывает обработку. Дубликаты — **отдельная информационная ось**, в
  `invoicesWithIssues` не попадают. Одно множество `invoiceIds` в
  `BatchAccumulator` служит и списком для отчёта, и детектором дубликатов:
  `Set.add` возвращает `false`, если фактура уже была.
- **Счётчики `ValidationOutput`:** считаются от уникальных фактур; инвариант
  `validInvoices + invoicesWithIssues = totalInvoices` защищён неизменяемостью —
  счётчики выставляются только в конструкторе.
- **`ValidationBatch.createdAt` — момент завершения обработки батча**, не начала:
  батч создаётся, когда все файлы уже обработаны.
- **`ValidationIssue.message`** — одна чистая фраза, без `\n` и без дублирования
  полей, которые есть отдельно.
- **API-ответы:** коллекции → `ListResponse<T>` с record-DTO; POST/PUT возвращают
  ресурс; DELETE → 204. **Все ошибки — единым форматом `ErrorResponse`.**
- **Веб-слой не пропускает свои типы вглубь:** `MultipartFile` дальше
  `XmlFileDataReader` не идёт, сервис принимает `XmlFileData`.
- **Контроллеры не перехватывают `Exception`.** Ловить можно только конкретное
  ожидаемое исключение; всё остальное — в `handleUnexpected` (500 + лог).
- **Магические числа-смещения не хардкодить:** длину префикса брать из именованной
  константы (`authHeader.substring(BEARER_PREFIX.length())`, не `substring(7)`).
- **Тестовые данные — синтетические.** Реальные конфиденциальные фактуры в
  репозиторий и в тесты не попадают; фикстуры полностью выдуманы.
- **Уникальность проверяем в коде** + констрейнты БД как страховка.
- **Три слоя валидации** с собственными сообщениями у ограничений, которые
  реально нарушаются.
- **Пароли — только хеш (BCrypt)**; доменная модель `User` пароль не содержит,
  в базу уходит результат `PasswordEncoder.encode`.
- **`handleUnexpected` обязан логировать** исключение.